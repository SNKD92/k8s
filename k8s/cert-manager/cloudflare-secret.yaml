# DEBT: This secret should be populated from GCP Secret Manager
# using:
# kubectl get secret cloudflare-api-token-secret -n cert-manager -o jsonpath='{.data.api-token}' | base64 --decode | \
# gcloud secrets versions add cloudflare-api-token --data-file=-
#
# Then recreate/update using:
# kubectl create secret generic cloudflare-api-token-secret \
#   -n cert-manager \
#   --from-literal=api-token="$(gcloud secrets versions access latest --secret=cloudflare-api-token)" \
#   --dry-run=client -o yaml | kubectl apply -f -

apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token-secret
  namespace: cert-manager
type: Opaque

# DEBT: placeholder only, must be replaced via kubectl create secret command
data:
  api-token: ""
